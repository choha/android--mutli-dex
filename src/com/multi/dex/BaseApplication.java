package com.multi.dex;import android.app.Application;import com.multi.option.DexPath;import com.multi.option.MultiDex;public class BaseApplication extends Application {    @Override    public void onCreate() {        super.onCreate();        // 需要需要复制，应该根据版本号之类的做区分        MultiDex multiDex = MultiDex.getInstance();        multiDex.addDexPath(new DexPath("dex/classes1.dex", true));        multiDex.addDexPath(new DexPath("dex/classes2.dex", true));        multiDex.loadMultiDex(this);    }}